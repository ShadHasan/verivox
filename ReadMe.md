
## Run the application
Goto `<root>/src` of the project at src 
### Setup
`yarnpkg install`
### Start application
`yarnpkg start &`

## To Test
Goto `<root>` of the project and run as below:
`test/run.sh` 

